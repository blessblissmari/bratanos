# BratanOS — default i3 config.
# Built on top of the upstream i3 default. Mod = Super (Windows key).

set $mod Mod4

font pango:JetBrainsMono Nerd Font 10

floating_modifier $mod
tiling_drag modifier titlebar

# launcher
bindsym $mod+d exec --no-startup-id rofi -show drun

# terminal
bindsym $mod+Return exec --no-startup-id alacritty

# kill focused window
bindsym $mod+Shift+q kill

# focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# splits
bindsym $mod+b split h
bindsym $mod+v split v

# layout
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+f fullscreen toggle

# floating
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle

# workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload / restart / exit
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'log out?' -B 'yes' 'i3-msg exit'"

# tidy shortcut — quick status check
bindsym $mod+Shift+t exec --no-startup-id alacritty -e bratan-tidy --status

# bratan-* app shortcuts (Mod+Shift+R is taken by 'restart i3' above)
bindsym Print              exec --no-startup-id bratan-recorder
bindsym $mod+Print         exec --no-startup-id bratan-recorder
bindsym $mod+Shift+m       exec --no-startup-id alacritty -e bratan-music
bindsym $mod+Shift+b       exec --no-startup-id bratan-browser
bindsym $mod+Shift+n       exec --no-startup-id thunar

# media keys (works when wf-recorder is recording too)
bindsym XF86AudioPlay exec --no-startup-id bratan-music play
bindsym XF86AudioNext exec --no-startup-id bratan-music next
bindsym XF86AudioPrev exec --no-startup-id bratan-music prev
bindsym XF86AudioStop exec --no-startup-id bratan-music stop
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioMute        exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle

# colors — BratanOS palette
set $bg     #1e1e2e
set $fg     #cdd6f4
set $accent #89b4fa
set $urgent #f38ba8
set $muted  #45475a

# class                 border  bg     text   indicator child_border
client.focused          $accent $accent $bg   $accent   $accent
client.focused_inactive $muted  $muted  $fg   $muted    $muted
client.unfocused        $bg     $bg     $fg   $bg       $bg
client.urgent           $urgent $urgent $bg   $urgent   $urgent

# autostart
exec --no-startup-id nm-applet
exec --no-startup-id picom --daemon
exec --no-startup-id dunst

# bar
bar {
    status_command i3status
    position top
    colors {
        background $bg
        statusline $fg
        separator  $muted
        focused_workspace  $accent $accent $bg
        active_workspace   $muted  $muted  $fg
        inactive_workspace $bg     $bg     $fg
        urgent_workspace   $urgent $urgent $bg
    }
}
